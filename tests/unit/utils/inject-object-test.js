import injectObject from '../../../utils/inject-object';
import { module, test } from 'qunit';

module('Unit | Utility | inject object');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = injectObject();
  assert.ok(result);
});
