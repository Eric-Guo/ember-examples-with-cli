 <table id="t01">
      <tr>
        <th>Company Name</th>
        <th>Headquaters</th>
        <th>revenue</th>
      </tr>
      {{#each model as |item|}}
        <tr>
          <td class="{{item.name}}">{{item.name}}</td>
          <td class="{{className}}">{{item.headquaters}}</td>
          <td>{{item.revenue}}</td>
        </tr>
      {{/each}}
</table>

<button {{action "toggleColor"}}> Change color </button>