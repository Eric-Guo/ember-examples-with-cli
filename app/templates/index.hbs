<table id="t01">
      <tr>
        <th>Company Name</th>
        <th>Headquaters</th>
        <th>revenue</th>
      </tr>
      {{#each model as |item|}}
        <tr>
          <td {{action "alert" "alert form company name" }}>{{item.name}}</td>
          <td {{action "alert" "alert form company headquaters" }}>{{item.headquaters}}</td>
          <td {{action "alert" "alert form revenue" }}>{{item.revenue}}</td>
        </tr>
      {{/each}}
</table>